import { ToggleChevronDirective } from './toggle-chevron.directive';

describe('ToggleChevronDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleChevronDirective();
    expect(directive).toBeTruthy();
  });
});
